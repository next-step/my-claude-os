# 시각 검증 데모 앱의 생성 산출물(스크린샷·기준선·diff)은 촬영마다 다시 만들어지므로 무시.
# (node_modules·dist·로그는 demo-app/.gitignore 가 따로 처리)
demo-app/screenshots/

# 시각 검증 스킬이 자동 기동한 dev 서버의 소유권 마커(런타임 상태) — SessionEnd 훅이 정리한다.
.claude/.visual-dev-server.pid
