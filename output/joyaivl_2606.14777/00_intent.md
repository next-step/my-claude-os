# 의도 스펙 (Intent Spec) — JoyAI-VL-Interaction: Real-Time Vision-Language Interaction Intelligence
- 링크: https://arxiv.org/abs/2606.14777
- slug: joyaivl_2606.14777
- 작성일: 2026-07-02

## 확정 의도
| 축 | 확정 내용 |
|---|---|
| What 대상·범위 | 원본 공식 레포(`jd-opensource/JoyAI-VL-Interaction`)를 1차 소스로. 재구현 아님. 파이프라인 6단계(analyzer→detail→code→code-run→html) 전부. |
| Where 실행·출력 위치 | 문서 산출물은 `output/joyaivl_2606.14777/`. 원본 코드 **실행은 사용자 터미널**(GPU 환경). 클로드 샌드박스에서 원본을 돌리지 않음. |
| Who 실행 주체 | code-run은 **명령·가이드만 클로드가 발행, 실행·관찰은 사용자**. 클로드 자동실행 금지. |
| How 형식·깊이 | 한국어. detail은 비전공자용 깊은 해설. |
| Why 성공 기준 | **원본 실제 구동 확인까지**가 최종 완료. 즉 report.html 렌더로 파이프라인은 끝나지만, 진짜 "됐다"는 사용자가 자기 터미널에서 원본을 돌려 05_run.md에 실측 로그를 붙이고 그 해석이 논문 동작과 일치할 때. |
| 제약 | 사용자 환경에 8B vLLM 서빙용 GPU가 없을 수 있음 → 원본 실제 구동은 별도 GPU 환경/시점에 사용자가 수행. Windows면 WSL2. |

## 단계별 지침 (해당 단계 에이전트가 기본값보다 우선 적용)
- **analyzer**: 원문 근거 기반. 제1저자 등 저자 메타데이터 정확히(arXiv 순서). 논문 라이선스(CC BY 4.0)와 코드 라이선스(Apache-2.0) 구분.
- **detail**: 비전공자 눈높이. 원문 비출처 예시/비유는 말미에 명시.
- **code**: 원본 레포 1차 분석. 논문↔코드 매핑 + 미구현(AdaCodec 등) 정직 표기.
- **code-run**: ★ **원본 레포를 사용자 터미널에서 돌릴 복붙 명령 블록 + 관찰 가이드**를 정본으로. 클로드 미실행. "실제 실행 로그"는 사용자가 붙일 때까지 "아직 미실행"으로 둔다. **추가로 GPU 없이 배선만 확인하는 오프라인 스모크 테스트(run/)도 함께 제공하되 "원본 아님"을 명시**(fallback).
- **html**: 위 산출물 병합. 수식/특수토큰(`</silence>` 등) 이스케이프 정확히.

## 남긴 가정 (미확정 → 합리적 기본값으로 채운 것)
- 출력 언어=한국어, detail 깊이=비전공자용(스킬 기본값).
- 원본 실제 구동은 사용자가 GPU 환경에서 나중에 수행(현재 세션에서 클로드가 대신 돌리지 않음).
- slug은 기존 폴더 `joyaivl_2606.14777` 재사용.
