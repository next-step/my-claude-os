---
name: plan
description: 확정된 PRD를 바탕으로 구현 Plan(설계)을 만든다. 기존 코드 재활용을 먼저 조사하고, 손댈 파일·구현 단계·테스트 전략을 설계한 뒤 사람 검토 게이트에서 멈춘다. 사용자가 "Plan 짜자", "설계하자", "/plan ..."처럼 PRD 이후 ② 설계 단계를 시작할 때 사용한다. 이 스킬은 OS.md 흐름의 ② 단계만 담당하며 ④ 구현은 범위 밖이다.
user-invocable: true
allowed-tools:
  - Task
  - Write
  - Read
  - Bash(ls:*)
  - Bash(test:*)
---

# /plan — PRD → Plan 단계 (② 설계)

[OS.md](../../../OS.md) 개발 흐름의 **② Plan 단계**만 담당하는 스킬이다.
(배경·원칙은 OS.md를 따른다. 여기서는 그 단계를 실제로 어떻게 굴리는지만 적는다.)

입력은 **`/prd`가 확정한 PRD**(`docs/prd/<slug>.md`)다. 출력은 구현 설계 문서
(`docs/plan/<slug>.md`)다. 설계가 끝나면 **사람 검토 게이트에서 멈춘다.**

전달된 인자: ``
- 인자가 있으면 → 기능 설명/slug로 사용한다.
- 인자가 없으면 → "어떤 기능의 Plan을 짤까요?"라고 한 줄로 묻고 시작한다.

이 스킬의 범위는 **② Plan(설계)까지**다. ④ 구현은 게이트 통과 후 별도 스킬에서 한다.

---

## 진행 순서

### [준비] slug 확인 + PRD 존재 확인

기능 설명에서 slug를 정하고, **확정 PRD가 있는지** `Bash(test:*)`로 확인한다.

```
docs/prd/<slug>.md     # 입력 (있어야 함)
docs/plan/<slug>.md    # 출력
```

- **PRD가 없으면** → "먼저 `/prd`로 PRD를 만들고 확정하세요"라고 안내하고 **멈춘다.**
  (Plan은 PRD 없이는 시작하지 않는다 — 흐름 순서를 건너뛰지 않는다.)
- **PRD가 있으면** → 그 PRD를 `Read`로 읽어 본문을 확보하고 다음으로 간다.

---

### Plan 생성 — Plan 서브에이전트 호출 (+ 기존 코드 재활용 조사)

`Task` 도구로 **Plan 설계 서브에이전트**를 띄운다. **핵심: "기존 코드를 먼저 조사하라"고 명시**한다.
(Plan 에이전트는 내부적으로 Explore 등으로 코드를 탐색할 수 있다.)

- `subagent_type`: `Plan`
- `description`: `"구현 Plan 설계"`
- `prompt`:

```
당신은 구현 Plan 설계자다. 아래 PRD를 어떻게 구현할지 설계하라.

[중요] 새 코드를 제안하기 전에, 이 저장소의 기존 코드/스킬/유틸을 먼저 조사해
재활용할 수 있는 것을 찾아라. (OS.md 원칙 2: 재활용 우선)

[중요] PRD에는 사람이 확정한 내용과, "열린 질문"에 사람이 채운 "답변:"이 들어 있다.
사람이 확정한 내용을 **최우선으로 반영**하라. 비어 있는 항목만 합리적으로 가정하고,
그 가정을 Plan에 명시하라. (사람의 결정을 임의로 뒤집지 마라.)

PRD (확정본):
<[준비]에서 읽은 확정 PRD 본문>

다음 형식의 마크다운으로만 답하라:
# Plan — <기능 이름>
## 재활용할 기존 코드 (조사 결과: 파일/스킬과 어떻게 쓸지)
## 손댈 파일 (신규/수정)
## 구현 단계 (순서대로)
## 테스트 전략 (단위/통합 — OS.md 원칙 3, 필수)
## 위험 요소 / 롤백
```

서브에이전트가 돌려준 Plan 본문을 **그대로** `docs/plan/<slug>.md` 에 `Write` 로 저장한다.

> 학습 포인트: Plan 서브가 코드 조사를 위해 다시 탐색하는 **중첩 협력**이 일어난다.
> AI가 AI를 부르고, 각자 다른 컨텍스트의 결과만 위로 올라온다.

---

### 검토 게이트 — ★반드시 멈춤★

Plan을 만들었으면 **요약해서 사람에게 보고하고 STOP 한다.**

```
✅ ② Plan 작성 완료 → docs/plan/<slug>.md

[Plan 요약]
- 재활용: ...
- 손댈 파일: ...
- 테스트: ...
- 위험: ...

여기는 검토 게이트입니다. Plan을 확인해 주세요.
- 수정이 필요하면 어디를 고칠지 알려주세요(다시 설계합니다).
- 승인하시면 ④ 구현 단계로 넘어갑니다.
```

**이 게이트를 넘어 임의로 구현(코드 작성)으로 진행하지 않는다.** (OS.md 원칙 1 — 가장 중요한 안전장치)
사용자가 명시적으로 "승인", "구현 시작"이라고 말하기 전에는 ④로 가지 않는다.

---

## 수용 기준 (이 스킬이 제대로 동작하는지 검증)

- [ ] PRD가 없으면 안내하고 멈춘다(Plan을 PRD 없이 시작하지 않는다).
- [ ] 진행 전 **확정 PRD를 `Read`로 읽어** 그 내용을 반영한다.
- [ ] Plan 생성이 **별도의 `Task` 서브에이전트 호출**로 일어나고, 기존 코드 재활용 조사가 반영된다.
- [ ] `docs/plan/<slug>.md` 가 생성된다.
- [ ] 검토 게이트에서 멈추고, 사용자 승인 없이 구현으로 넘어가지 않는다.

---

## 안전 규칙

- **게이트를 넘지 않는다.** 사람의 승인 전에는 절대 ④ 구현으로 가지 않는다.
- **모든 산출물은 저장소 안에** 둔다(`docs/plan/`). (CLAUDE.md 규칙 1)
- 서브에이전트가 빈 결과/오류를 돌려주면 그대로 알리고 멈춘다. 추측으로 채우지 않는다.
- 이 스킬은 **읽기·문서 작성**만 한다. 코드 파일을 수정하지 않는다(그건 ④ 구현의 몫).
