# 내 OS 설계: 주식/투자 OS

> 4주 동안 만들 "나만의 클로드 코드 OS" 설계 문서. 진행하며 계속 업데이트하는 살아있는 문서다.
> 배경 지식은 [onboarding.md](./onboarding.md) 참고.

## 주제 & 동기

- **주제**: 주식/투자 OS
- **최종 목표**: 알아서 매매까지 해주는 **자동매매 에이전트**
- **동기**: 투자 판단과 실행을 AI에게 위임해, 내 투자 성향에 맞게 자동으로 굴러가는 체계를 갖고 싶다.

## 핵심 원칙: 동작하는 것부터, 신뢰는 단계적으로

자동매매는 **종착지이지 출발점이 아니다.** 온보딩의 권장사항("완벽하지 않아도 동작하는 OS를 먼저, 이후 지속 업그레이드")과 현실적 위험(실제 돈·실매매 API·규제)을 고려해, **사람이 승인하는 조언가(1단계)부터** 시작해 신뢰를 쌓으며 자동화 수준을 올린다.

## 단계별 로드맵

**이번 4주 강의의 범위는 1단계 완성**이다. 2·3단계는 강의가 끝난 뒤 혼자 얹어볼 **미래 로드맵**으로만 남겨둔다.

| 단계 | 사람의 역할 | AI의 역할 | 위험 | 상태 |
|------|------------|----------|------|------|
| **1단계 (MVP)** | 투자 성향 제공 + **매매 최종 승인** | 분석·조언·주문서 초안 작성 | 낮음 (모의/수동) | 🟢 4주 강의 목표 |
| **2단계** | 전략·한도 설정, 예외만 개입 | 조건 충족 시 주문 제안, 승인 후 실행 | 중간 | ⚪ 강의 후 미래 |
| **3단계 (목표)** | 가드레일·리뷰만 | 탐색→판단→**자동 매매**→결과 학습 | 높음 | ⚪ 강의 후 미래 |

## 공통 골격 적용 (1단계 기준)

온보딩의 공통 골격 `의도 공유 → 실행 → 피드백 → 반복`을 1단계에 매핑한 것이다.

```
[사람+AI] 의도 공유      투자 성향·목표·제약(종목군, 리스크 한도) 인터뷰
        ↓
[AI]      실행           시장·종목 분석 → 매매 조언 → 주문서 초안 작성
        ↓
[사람]    의사결정        조언 검토 후 매매 승인/거절 (실제 주문은 사람이)
        ↓
[AI]      피드백          결과 기록 → 동향 데이터 확보 → 다음 조언에 반영
        └──────────────── ↻ 반복 ────────────────┘
```

## 1단계 MVP 범위 (4주 안에 동작 목표)

- [ ] 투자 성향 인터뷰 → 프로필 저장 (리스크 성향, 관심 종목군, 한도 등)
- [ ] 종목/시장 데이터 수집 (어떤 소스를 쓸지 미정 — 아래 열린 질문)
- [ ] 분석 → 매매 조언 생성 (사유 포함)
- [ ] 주문서 초안 제시 (종목/수량/가격/근거)
- [ ] 사람이 승인/거절 → 결과 기록
- [ ] 결과·동향을 다음 조언에 반영하는 루프

> 실매매 대신 **모의투자/페이퍼트레이딩**으로 검증 후, 신뢰가 쌓이면 단계 상향.

## 4주 학습 계획 (1단계를 매주 한 겹씩 완성)

자동화 단계를 올리는 게 아니라, **하나의 1단계 조언가 OS**를 매주 그 주의 개념으로 채워 완성한다.
각 주차의 구체적 계획은 **해당 주차 강의를 듣고 나서** 이 섹션에 채운다. (미리 추측해 적지 않는다.)
4주가 끝나면 **완성된 1단계 조언가 OS** 하나가 남는 것이 목표.

| 주차 | 개념 | 1단계에서 만든 것 | 상태 |
|------|------|------------------|------|
| **1주차** | 에이전틱 | _강의 수강 후 작성_ | ⚪ 대기 |
| **2주차** | 컨텍스트 | _강의 수강 후 작성_ | ⚪ 대기 |
| **3주차** | 루프 | _강의 수강 후 작성_ | ⚪ 대기 |
| **4주차** | 하네스 | _강의 수강 후 작성_ | ⚪ 대기 |

## 가드레일 (안전 원칙)

- 1단계에서는 **실제 주문을 AI가 직접 실행하지 않는다.** (사람 승인 필수)
- 손실 한도·종목당 비중 등 **한도를 명시**하고 그 안에서만 조언.
- 모든 매매 조언에 **근거를 남겨** 나중에 복기 가능하게 한다.

## 회사 분석 스킬 (`analyze-company`)

1단계 MVP의 "종목/시장 데이터 수집 → 분석" 부분을 담당하는 첫 스킬.
회사 이름을 받아 서브 에이전트들이 데이터를 **병렬 수집**하고 메인이 **종합**한다.

- **구조**: 멀티 에이전트 (수집 전문 서브 에이전트 3 + 종합 메인 1)
  - `stock-trend-researcher` — 최근 1년 주가 흐름
  - `company-news-researcher` — 회사 관련 뉴스
  - `market-news-researcher` — 대한민국 경제(거시) 뉴스
- **수집 방식**: 웹검색(WebSearch/WebFetch) 기반. *왜:* API 키 발급 없이 바로 시작 가능, 1단계 검증에 충분. 정확도가 필요해지면 DART·증권사 API로 단계적 교체.
- **데이터 범위(현재)**: 위 3가지로 고정. *왜:* 동작하는 것부터 만들고, 아래 후보들은 **서브 에이전트를 추가하는 방식**으로 나중에 확장.

### 확장 후보 데이터 (우선순위순, 강의 후/필요 시 추가)

투자 분석의 고전적 5축은 **기술적 / 펀더멘털 / 수급 / 심리 / 거시**. 현재 스킬은 기술적(주가)·심리(뉴스)·거시(경제뉴스)를 다루고, **펀더멘털·수급**이 비어 있다. 채울 후보:

| 순위 | 데이터 | 왜 필요한가 | 메모 |
|------|--------|------------|------|
| 1 | **재무제표/펀더멘털** (매출·영업이익·ROE, PER·PBR·배당) | 주가는 "시장의 평가", 재무는 "그럴 만한가". 둘을 대조해야 고/저평가 판단이 선다. | 가장 큰 공백 |
| 2 | **공시 (DART)** (실적·증자·자사주·배당·내부자거래) | 무료+구조화+공신력 갖춘 1차 자료. 뉴스보다 신뢰도 높음. | DART API 후보 |
| 3 | **수급** (외국인/기관/개인 매매) | 한국 단기 주가의 핵심 동인. "왜 움직였나"의 절반. | |
| 4 | **동종업계/경쟁사 비교** | 단독 PER·성장률은 의미가 약함. 업계 평균이 기준점을 준다. | |
| 5 | (선택) **애널리스트 컨센서스/목표주가**, **구체 거시 수치**(환율·금리·유가) | 외부 시각·정량 거시 보강. | |

> 데이터 선택 기준: **"이 데이터가 매수/매도/보류 판단을 바꾸는가?"** — 아니면 넣지 않는다.
> 위 축들은 서로 직교(orthogonal)해서, 그대로 서브 에이전트 하나씩으로 매핑된다.

## 종목 추천 스킬 (`recommend-stocks`, 설계 확정)

코스피 전체에서 **안정성 우선**으로 소수 종목을 추려, 각각의 기대치·손절가·진입가까지 내주는 스킬. `analyze-company`를 깔때기의 마지막 단계로 **재사용**한다.

### 파이프라인 (깔때기)

```
코스피 전체(~1000)
   ↓  1차 필터  (네이버 시총 랭킹 표의 일괄 컬럼: 시총·거래대금·PER·ROE)  — 싼 하드컷
  ~50
   ↓  2차 필터  (좁혀진 50개만 네이버 개별 재무 탭: 흑자추세·부채비율) → 안정성 점수로 순위
  ~10
   ↓  정밀 분석  (analyze-company 를 10개 각각에 실행)
최종 추천  =  종목명 + 주가 기대치 + 손절가 + 진입가 + 안정성 점수,  안정성 우선 정렬
```

- **왜 2겹으로 거르나:** 1000개 전부의 개별 페이지를 긁는 건 느리고 과하다. 싼 일괄 컬럼으로 먼저 줄이고(1차), 줄어든 것만 깊게 본다(2차) — 깔때기 원리를 필터 안에서 한 번 더 적용.
- **왜 무거운 `analyze-company`는 마지막에:** 종목당 서브에이전트 3개가 웹검색을 돈다. 1000개에 돌리면 불가능(시간·비용). ~10개로 좁힌 뒤에만 실행.
- **안정성 우선 정렬:** 상승 기대치가 높다고 위로 올리지 않는다. "A: 상승15%·불안정 < B: 상승8%·안정"이면 B를 더 추천. → 안정성 점수 = 정렬 기준, 상승 기대치 = 산출물(동점 보조).

### 데이터 소스 결정 (정형)

- **1·2차 필터 = 네이버 금융 스크래핑.** *왜:* 네이버는 FnGuide 재무까지 보여줘 **시세·시총·PER·ROE + 요약 재무(부채비율·흑자추세)** 를 한 곳에서 제공 → 필터에 필요한 데이터를 거의 다 커버. 키 불필요, 당장 동작(“동작하는 것부터” 원칙).
- **pykrx 는 탈락.** *왜:* 시장 전체 일괄 조회 함수가 모두 `data.krx.co.kr`(403/`LOGOUT`)에 막혀 빈 데이터로 크래시(2026-06-30 직접 테스트로 확인, 최신 1.0.51·국내 IP에서도 동일). 정작 1차 필터에 필요한 그 기능이 막혀 토대로 부적합. (개별 종목 시세만 동작 → 1000개엔 안 맞음)
- **DART 는 도입 보류 → 3단계 정밀분석에 배치.** *왜:* 필터 단계는 네이버로 충분. DART의 강점(공시 "사건": 증자·자사주·공급계약·내부자거래, 수치의 공신력)은 **한 종목을 깊게 볼 때** 판단을 바꾸므로, `analyze-company`에 **서브에이전트(`fundamentals`/`disclosure`)를 하나 더 붙이는 방식**으로 후행 도입. OS.md 80번 줄의 "웹검색으로 시작 → 정확도 필요 시 DART로 단계적 교체" 철학과 일치.

### `analyze-company` 확장 (이 스킬을 위해 필요)

추천 출력에 **기대치·손절가·진입가**가 생기면서, 그 숫자를 근거 있게 내려면 현재 3축(주가·뉴스·거시)으론 부족. 다음이 must-have로 승격:
- **컨센서스 목표주가 + 동종업계 PER 비교** → 기대치 근거 (현재 없음, 추가 필요)
- **기술적 지지/저항·변동성(ATR)** → 손절가·진입가 근거 (`stock-trend-researcher` 확장)
- **펀더멘털/재무(DART)** → 안정성 점수 근거 (추천 로직과 일관)

## 회고 데이터 관리 (설계 방향)

회고 스킬이 "필터 기준이 타당했나 / 빠뜨린 좋은 종목은 / 기대치 도달했나"를 따지려면, 추천 시점의 기록을 남겨야 한다. 원칙:

1. **입력+출력 둘 다 스냅샷.** 출력(추천 종목·가격)뿐 아니라 **필터 조건·탈락 종목 표본**도 저장 — "왜 이 종목을 안 골랐나"를 회고하려면 필수.
2. **append-only(불변).** 과거 추천은 수정 금지. 회고는 기존 기록에 "평가"만 덧붙인다.
3. **그때의 가격을 박제.** 추천 시점 진입가·목표가·손절가를 저장 → 나중에 현재가와 대조해 status(목표달성/손절/보유중) 판정.
4. **저장 위치(잠정):** 프로젝트 내 `data/recommendations/YYYY-MM-DD-run.md` (frontmatter=정량 + 본문=근거 하이브리드). 상세 포맷은 추천 스킬 구현 시 확정.

## 열린 질문 (정해야 할 것들)

- 모의투자 환경: 증권사 모의계좌 vs 자체 페이퍼트레이딩 시뮬레이터? (KIS API는 이때 다시 후보)
- 대상 시장: 국내 주식 / 미국 주식 / 코인 중 무엇으로 시작? (현재 분석·추천 스킬은 국내장 가정)
- 컨센서스 목표주가 소스: 1단계는 웹검색으로 보강 예정 — 정형화는 언제?
- 회고 기록 포맷의 구체 스키마(필드·status 전이)는 추천 스킬 구현 때 확정.

## 진행 로그

- 2026-06-22: 주제 확정(주식/투자), 최종 목표(자동매매) 및 1단계 조언가부터 시작하기로 결정.
- 2026-06-22: 이번 4주 강의 범위를 1단계 완성으로 한정. 주차별 개념(에이전틱·컨텍스트·루프·하네스)에 맞춰 1단계를 한 겹씩 완성하는 학습 계획 추가.
- 2026-06-22: 주차별 구체 계획은 미리 추측하지 않고 **해당 주차 강의를 듣고 작성**하기로 변경(추측 내용 제거, 빈 표로 전환).
- 2026-06-25: 첫 스킬 `analyze-company` 추가. 멀티 에이전트(주가·회사뉴스·경제뉴스 서브 3 + 종합 메인) 구조, 웹검색 기반으로 결정. 데이터는 우선 3가지로 고정하고 펀더멘털·공시·수급은 서브 에이전트 추가로 확장하기로 함.
- 2026-06-25: 확장 후보 데이터(펀더멘털·공시·수급·동종업계·컨센서스)를 이유와 우선순위와 함께 OS.md에 정리(나중에 다시 고민하지 않도록).
- 2026-06-30: 넥스트스텝 코드리뷰 요청 가이드(review-step1~3)의 git 절차를 행동형 스킬로 추가. `nextstep-pr`(push→제목·본문/저장소·브랜치 확인→PR 생성 링크 제공, PR 생성은 브라우저)와 `nextstep-next-step`(merge 후 upstream rebase 동기화→다음 step 브랜치 생성). 커밋은 기존 `commit` 스킬 재사용(중복 방지). 가이드를 매번 찾아보지 않으려는 목적. 투자 OS 본류는 아니고 개발 워크플로 보조 스킬.
- 2026-06-30: 종목 추천 스킬(`recommend-stocks`) 설계 확정. 깔때기(1000 →[네이버 1차 일괄컬럼]→ ~50 →[네이버 2차 개별재무]→ ~10 →[analyze-company 정밀]→ 기대치/손절/진입, 안정성 우선 정렬). 정형 데이터 소스 결정: **필터=네이버 스크래핑**(시세+요약재무 한 곳에 있음), **pykrx 탈락**(시장전체 일괄조회가 data.krx.co.kr 403/LOGOUT으로 막힘 — 직접 테스트 확인), **DART는 도입 보류→정밀분석 단계에 서브에이전트로 후행**. analyze-company 확장(컨센서스·지지저항·펀더멘털)을 must-have로 승격. 회고 데이터는 입력+출력 append-only 스냅샷 원칙 기록. 데이터 소스 열린 질문 닫음.
- 2026-06-30: 종목 추천 스킬(`recommend-stocks`) **뼈대 작성**. 1차 스크리너 `scripts/screen_kospi.py`(네이버 코스피 시총 표 파싱, 표준 라이브러리만, 하드컷 기준=시총≥5천억·거래대금≥10억·0<PER≤25·ROE≥5%, JSON 출력) 구현·동작 확인. SKILL.md에 파이프라인·필터 기준·실행 절차 정리. 2차 안정성 스코어러/analyze-company 연동/회고 스냅샷은 TODO로 남김.
- 2026-06-30: `recommend-stocks` 남은 작업 구현. (2차) `score_stocks.py` — 개별 종목 재무(네이버 기업실적분석, UTF-8)에서 부채비율·영업이익·ROE를 파싱, 하드컷(부채비율≤150·최근연도 흑자)+안정성 점수(재무건전성35·이익안정성35·ROE15·PER15)로 상위 N 선정. (5차) `save_run.py` — 입력+출력 append-only 스냅샷을 `data/recommendations/YYYY-MM-DD-run.md`(frontmatter+본문)로 저장, 같은 날 재실행은 -N 신규파일로 기존 보존. (3차) analyze-company 연동은 메인 에이전트가 상위 10개에 스킬 호출 후 진입/목표/손절을 종합하는 절차로 SKILL.md에 정의. 1차→2차 파이프 동작 확인(기아·오리온 등 저부채·견조 ROE 상위). analyze-company 축 보강·주가변동성/배당 가점은 향후 보강으로 남김.
- 2026-06-30: `analyze-company`가 recommend-stocks 3단계 요구(진입/목표/손절)에 맞도록 수정. (1) `stock-trend-researcher`에 주요 지지·저항선과 컨센서스 목표주가(있으면) 수집 추가(수집만, 판단 안 함). (2) analyze-company SKILL.md에 3.5단계 "매매 가격대 산출"(진입=지지선 부근, 손절=지지선 아래 −5~8%, 목표=컨센서스 or 저항선 보수적)과 출력 섹션 추가 — 판단=종합은 메인 몫이라 구조 원칙 유지. 재료 없으면 "산출 불가"로 명시. **아직 end-to-end 실행 검증은 안 함**(다음 차례).
- 2026-06-30: `analyze-company`를 기아로 end-to-end 실행해 수정 검증 완료. 세 서브에이전트 병렬 동작, 새 출력 `매매 가격대`가 실데이터로 채워짐(진입 167,000·목표 212,000(+27%)·손절 155,000). 컨센서스 목표주가 수집은 견고했으나 **명시적 지지/저항선은 "확인 불가"라 52주 고저로 대체** → 진입/손절 근거가 약함이 실증됨. OS.md 보강 항목 "지지저항 축 보강"의 필요성 확인. recommend-stocks 3단계 [x]로 갱신.
- 2026-06-30: **현재가 앵커 결함 수정**(사용자가 실제 시세 137,000인데 진입가 167,000으로 나온 걸 지적). 원인: analyze-company가 현재가를 웹검색에 의존 → 출처 충돌(170,200 vs 140,600) 시 엉뚱한 값(170,200) 단독 채택. 교훈: **정성 정보=웹검색 OK, 정밀 시세=결정론적 소스 필수.** 수정: (1) `analyze-company/scripts/quote.py` 신규 — 네이버 개별 페이지에서 현재가·52주 고저·거래량을 직접 읽고 이름→코드 변환(모바일 autoComplete)도 지원. (2) analyze-company 2.5단계 "현재가 확정"으로 quote.py 사용, 3.5단계 진입/손절을 quote.py 현재가에 앵커(진입가>현재가면 오류로 재검토). (3) stock-trend-researcher: 현재가 충돌 시 단독 채택 금지·네이버 우선. (4) recommend-stocks score_stocks.py 출력에 검증된 price 추가→3단계 전달. 검증: 기아 앵커 170,200→139,000 정정, 진입가 167,000→~137,000(현재가 이하)로 정상화. 남은 약점: 정밀 지지/저항은 여전히 52주 고저 의존(OHLCV 기반 계산은 향후 보강).
